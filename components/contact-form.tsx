"use client";
import{useState}from"react";import{useForm}from"react-hook-form";import{zodResolver}from"@hookform/resolvers/zod";import{z}from"zod";import{ArrowRight,Check}from"lucide-react";
const schema=z.object({name:z.string().min(2),email:z.string().email(),phone:z.string().optional(),company:z.string().optional(),budget:z.string().min(1),service:z.string().min(1),message:z.string().min(20),website:z.string().max(0).optional()});type Form=z.infer<typeof schema>;
export function ContactForm(){const[done,setDone]=useState(false);const{register,handleSubmit,formState:{errors,isSubmitting}}=useForm<Form>({resolver:zodResolver(schema)});async function submit(data:Form){const res=await fetch("/api/contact",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});if(!res.ok)throw new Error("Unable to send");setDone(true)}if(done)return <div className="border border-gold/30 p-10"><Check className="text-gold"/><h3 className="mt-5 font-serif text-4xl">Message received.</h3><p className="mt-3 text-muted">We’ll reply within two business days.</p></div>;return <form onSubmit={handleSubmit(submit)} className="grid gap-x-8 md:grid-cols-2"><input {...register("website")} tabIndex={-1} autoComplete="off" className="hidden"/><label><span className="sr-only">Name</span><input className="field" placeholder="Name *" {...register("name")}/>{errors.name&&<small className="text-red-300">Please enter your name.</small>}</label><label><span className="sr-only">Email</span><input className="field" placeholder="Email *" {...register("email")}/></label><input className="field" placeholder="Phone" {...register("phone")}/><input className="field" placeholder="Company" {...register("company")}/><select className="field" defaultValue="" {...register("budget")}><option value="" disabled>Project budget *</option><option>$10k–$25k</option><option>$25k–$50k</option><option>$50k–$100k</option><option>$100k+</option></select><select className="field" defaultValue="" {...register("service")}><option value="" disabled>Primary service *</option><option>Website Design</option><option>Software Development</option><option>AI Automation</option><option>Brand Strategy</option></select><textarea className="field min-h-36 md:col-span-2" placeholder="Tell us about the ambition behind the project *" {...register("message")}/><div className="mt-8 md:col-span-2"><button disabled={isSubmitting} className="button">{isSubmitting?"Sending…":"Send inquiry"}<ArrowRight size={15}/></button></div></form>}
