"use client";import{useState}from"react";const slots=["09:00","10:30","12:00","14:00","15:30","17:00"];export function BookingForm(){const[done,setDone]=useState(false);const[loading,setLoading]=useState(false);async function submit(fd:FormData){setLoading(true);const data=Object.fromEntries(fd);data.timezone=Intl.DateTimeFormat().resolvedOptions().timeZone;const r=await fetch("/api/bookings",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});setLoading(false);if(r.ok)setDone(true)}if(done)return <div className="border border-gold/30 p-10"><h2 className="font-serif text-4xl">Your call is reserved.</h2><p className="mt-3 text-muted">A confirmation is on its way.</p></div>;return <form action={submit} className="grid gap-5 md:grid-cols-2"><input required name="name" className="field" placeholder="Name"/><input required type="email" name="email" className="field" placeholder="Email"/><input name="company" className="field" placeholder="Company"/><input required name="date" type="date" min={new Date().toISOString().slice(0,10)} className="field [color-scheme:dark]"/><select name="time" required className="field"><option value="">Choose a time</option>{slots.map(x=><option key={x}>{x}</option>)}</select><textarea name="notes" className="field md:col-span-2" placeholder="Anything we should know?"/><button disabled={loading} className="button mt-5 md:col-span-2">{loading?"Reserving…":"Reserve call"}</button></form>}
