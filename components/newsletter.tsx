"use client";import{useState}from"react";import{ArrowRight}from"lucide-react";
export function Newsletter(){const[message,setMessage]=useState("");async function submit(formData:FormData){const email=formData.get("email");const res=await fetch("/api/newsletter",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email})});setMessage(res.ok?"You’re on the list.":"Please try again.")}return <form action={submit} className="mt-6 max-w-md"><div className="flex border-b hairline"><input required type="email" name="email" aria-label="Email" placeholder="Your email address" className="w-full bg-transparent py-3 text-sm outline-none"/><button aria-label="Subscribe"><ArrowRight size={18}/></button></div>{message&&<p className="mt-2 text-xs text-gold">{message}</p>}</form>}
