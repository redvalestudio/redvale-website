"use client";
import Link from "next/link";import {Menu,X,ArrowUpRight} from "lucide-react";import {useState} from "react";
const links=[["Work","/work"],["Services","/services"],["About","/about"],["Process","/process"]];
export function Header(){const[open,setOpen]=useState(false);return <header className="fixed inset-x-0 top-0 z-50 mix-blend-difference"><div className="container flex h-24 items-center justify-between"><Link href="/" className="relative z-10 text-[11px] font-semibold tracking-[.24em]">RED VALE <span className="text-white/50">STUDIO</span></Link><nav className="hidden items-center gap-10 md:flex">{links.map(([n,h])=><Link key={h} href={h} className="text-[10px] uppercase tracking-[.2em] text-white/60 transition hover:text-white">{n}</Link>)}<Link href="/contact" className="border-b border-white/50 pb-1 text-[10px] uppercase tracking-[.2em]">Start a project</Link></nav><button aria-label="Toggle menu" className="relative z-10 md:hidden" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></div>{open&&<nav className="fixed inset-0 flex min-h-screen flex-col justify-center gap-5 bg-ink px-7 mix-blend-normal md:hidden">{[...links,["Contact","/contact"]].map(([n,h])=><Link onClick={()=>setOpen(false)} key={h} href={h} className="font-serif text-5xl">{n}</Link>)}</nav>}</header>}
