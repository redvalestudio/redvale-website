"use client";
import{useEffect,useState}from"react";import{motion,AnimatePresence}from"framer-motion";
export function Loader(){const[show,setShow]=useState(true);useEffect(()=>{const seen=sessionStorage.getItem("rvs-seen");if(seen){setShow(false);return}const id=setTimeout(()=>{setShow(false);sessionStorage.setItem("rvs-seen","1")},1700);return()=>clearTimeout(id)},[]);return <AnimatePresence>{show&&<motion.div exit={{y:"-100%"}} transition={{duration:.9,ease:[.76,0,.24,1]}} className="fixed inset-0 z-[100] grid place-items-center bg-ink"><div className="text-center"><motion.p initial={{opacity:0}} animate={{opacity:1}} className="font-serif text-3xl tracking-[.2em]">RED VALE</motion.p><div className="mt-7 h-px w-44 overflow-hidden bg-white/10"><motion.div initial={{x:"-100%"}} animate={{x:0}} transition={{duration:1.2,ease:"easeInOut"}} className="h-full bg-gold"/></div></div></motion.div>}</AnimatePresence>}
