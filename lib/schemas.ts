import{z}from"zod";export const contactSchema=z.object({name:z.string().min(2).max(100),email:z.string().email(),phone:z.string().max(40).optional(),company:z.string().max(100).optional(),budget:z.string().min(1).max(40),service:z.string().min(1).max(80),message:z.string().min(20).max(4000),website:z.string().max(0).optional()});export const bookingSchema=z.object({name:z.string().min(2),email:z.string().email(),company:z.string().optional(),date:z.string().date(),time:z.string().regex(/^\d{2}:\d{2}$/),timezone:z.string().min(2).max(100),notes:z.string().max(1000).optional()});
