import{Resend}from"resend";export async function sendEmail(subject:string,html:string,to?:string){if(!process.env.RESEND_API_KEY)return;const resend=new Resend(process.env.RESEND_API_KEY);await resend.emails.send({from:process.env.RESEND_FROM_EMAIL||"Red Vale Studio <onboarding@resend.dev>",to:to||process.env.CONTACT_NOTIFICATION_EMAIL||"redvalestudio.work@gmail.com",subject,html})}
