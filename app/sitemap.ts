import type{MetadataRoute}from"next";import{projects}from"@/lib/data";export default function sitemap():MetadataRoute.Sitemap{const b=process.env.NEXT_PUBLIC_SITE_URL||"http://localhost:3000";return["","/work","/services","/about","/process","/contact","/book","/privacy","/terms",...projects.map(p=>`/work/${p.slug}`)].map(url=>({url:b+url,lastModified:new Date(),changeFrequency:url===""?"weekly":"monthly",priority:url===""?1:.7}))}
