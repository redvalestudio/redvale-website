import{notFound}from"next/navigation";import Image from"next/image";import Link from"next/link";import{ArrowLeft}from"lucide-react";import{projects}from"@/lib/data";
export function generateStaticParams(){return projects.map(p=>({slug:p.slug}))}export default async function Case({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const p=projects.find(x=>x.slug===slug);if(!p)notFound();return <><section className="pt-40"><div className="container"><Link href="/work" className="flex items-center gap-2 text-xs uppercase tracking-[.16em] text-muted"><ArrowLeft size={14}/> All work</Link><p className="eyebrow mt-16">{p.tag}</p><h1 className="display mt-7 text-[clamp(5rem,12vw,11rem)]">{p.title}</h1><div className="mt-14 grid gap-8 border-t hairline py-7 text-sm text-muted md:grid-cols-3"><p>Engagement<br/><span className="text-paper">Strategy, UX, Development</span></p><p>Released<br/><span className="text-paper">{p.year}</span></p><p>Focus<br/><span className="text-paper">Product clarity and trust</span></p></div></div><div className="relative aspect-[16/8]"><Image fill priority src={p.image} alt={p.title} className="object-cover" sizes="100vw"/></div></section><section className="section"><div className="container grid gap-12 md:grid-cols-[1fr_2fr]"><p className="eyebrow">The challenge</p><div><h2 className="font-serif text-5xl md:text-7xl">Making intelligence feel human.</h2><p className="mt-8 max-w-2xl text-base leading-8 text-muted">The product needed to explain a sophisticated system without making people feel like they were operating one. We reduced the experience to a calm sequence of clear choices, meaningful feedback, and a visual language built around confidence.</p><div className="mt-16 grid gap-px bg-white/10 md:grid-cols-3">{[["42%","Clearer task completion"],["2.1×","Faster onboarding"],["AA","Accessibility target"]].map(x=><div className="bg-panel p-8" key={x[0]}><strong className="font-serif text-5xl text-gold">{x[0]}</strong><p className="mt-3 text-xs text-muted">{x[1]}</p></div>)}</div></div></div></section></>}
