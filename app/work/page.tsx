import Image from"next/image";import Link from"next/link";import{ArrowUpRight}from"lucide-react";import{PageIntro}from"@/components/page-intro";import{projects}from"@/lib/data";
export const metadata={title:"Selected Work"};export default function Work(){return <><PageIntro kicker="Selected work" title="Proof, not promises." copy="Digital products built at the intersection of commercial clarity, precise craft, and ambitious technology."/><section className="section"><div className="container space-y-24">{projects.map((p,i)=><Link href={`/work/${p.slug}`} key={p.slug} className="group block"><div className="project-media relative aspect-[16/8]"><Image fill src={p.image} alt={p.title} className="object-cover" sizes="100vw"/></div><div className="mt-6 flex items-start justify-between"><div><p className="text-[10px] uppercase tracking-[.2em] text-gold">{p.tag}</p><h2 className="mt-2 font-serif text-5xl md:text-7xl">{p.title}</h2></div><ArrowUpRight className="mt-3 transition group-hover:-translate-y-1 group-hover:translate-x-1"/></div></Link>)}</div></section></>}
