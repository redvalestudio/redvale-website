import{NextResponse}from"next/server";import{z}from"zod";import{adminDb}from"@/lib/supabase";import{sendEmail}from"@/lib/email";export async function POST(req:Request){try{const{email}=z.object({email:z.string().email()}).parse(await req.json());const db=adminDb();if(db)await db.from("newsletter").upsert({email},{onConflict:"email"});await sendEmail("Welcome to Red Vale field notes","<p>Thoughtful notes are on their way. Welcome.</p>",email);return NextResponse.json({ok:true})}catch{return NextResponse.json({error:"Invalid email"},{status:400})}}
