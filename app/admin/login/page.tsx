"use client";import{useRouter}from"next/navigation";export default function Login(){const router=useRouter();async function submit(fd:FormData){const r=await fetch("/api/admin/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({password:fd.get("password")})});if(r.ok)router.push("/admin")}return <section className="grid min-h-screen place-items-center"><form action={submit} className="w-full max-w-sm border hairline p-8"><p className="eyebrow">Private</p><h1 className="mt-5 font-serif text-4xl">Studio admin</h1><input required type="password" name="password" className="field mt-8" placeholder="Password"/><button className="button mt-8 w-full">Sign in</button></form></section>}
